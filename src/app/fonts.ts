import { Zen_Loop, Raleway } from "next/font/google";
 
export const zen = Zen_Loop({
    subsets: ["latin"],
    weight: "400"
});
export const raleway = Raleway({
  subsets: ["latin"],
  weight: ["300", "400"]
});